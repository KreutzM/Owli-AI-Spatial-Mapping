# Synthetic fixtures

The first algorithmic slice should add generated, human-inspectable fixtures here. Do not add an arbitrary camera video as the core test oracle; use explicit intrinsics, poses, depth, confidence and expected geometry.
