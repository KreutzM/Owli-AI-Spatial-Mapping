# Test data

- `synthetic/`: tiny deterministic fixtures safe for Git.
- `recorded/`: local or access-controlled real-device recordings; ignored by default.

No fixture should be added without a manifest and privacy review. See `docs/DATASET_POLICY.md`.
