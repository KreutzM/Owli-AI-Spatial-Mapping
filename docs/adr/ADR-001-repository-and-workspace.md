# ADR-001: Independent repository plus workspace submodule

**Status:** Accepted for bootstrap

The spatial prototype is developed in an independent repository to isolate experimental ARCore, sensor, mapping, and data concerns. After a green reviewed bootstrap, it may be referenced as `spatial/` from `Owli-AI-Assist-WS`. The workspace remains coordination-only.
